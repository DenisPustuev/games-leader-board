# Games leaderboards widget

## Gulp tasks

* default (just `gulp`): starts browsersync and compiles build each time code has changed
* deploy (`gulp deploy`): pushes everything in the `public` folder  to `gh-pages` branch on github.
